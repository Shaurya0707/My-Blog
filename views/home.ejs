<!DOCTYPE html>
<html lang="en">
<head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Home</title>
</head>
<body>

<%- include("partials/header.ejs") %>

<h1 style="font-size: 40px;
font-weight: 600;
background-image: linear-gradient(45deg,#1abc9c,#ff7e34);
color: transparent;
background-clip: text;
-webkit-background-clip: text;">Home</h1>
<p><%= starting %></p>

   <% posts.forEach(post => { %>

         <h3 style="font-size: 20px;
         font-weight: 600;
         background-image: linear-gradient(45deg,#ee4b2b, #553c9a);
         color: transparent;
         background-clip: text;
         -webkit-background-clip: text;">
         
         <%= post.titles %></h3>
         <p><%= post.contents.substring(0,100) + "..." %><a href="/blog/<%= post.titles %>">Read More</a></p>
   <% }); %>

<%- include("partials/footer.ejs") %>
</body>
</html>

